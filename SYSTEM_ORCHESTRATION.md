# 🧠 NEXUM v1.0 - GLOBAL SYSTEM ORCHESTRATION

## 🎯 OBJECTIVE FOR CLAUDE (ENGINEER & DESIGNER)
You are tasked to audit, complete, and stylize the NEXUM Ecosystem. 
NEXUM is a unified AI environment consisting of:
1. **Cloud Backend (Railway)**: The brain and database.
2. **NEXUM Browser (Electron)**: A free, AI-native web portal.
3. **NEXUM Agent (Python)**: Local PC executor.
4. **Telegram Bot (Grammy)**: Mobile hub and control unit.

---

## 🔍 AUDIT CHECKLIST (MANDATORY)
1. **WebSocket Auth**: Verify that the token generated by `/link_pc` in Telegram correctly authenticates the `pc_agent` and `nexum_browser`.
2. **Database Consistency**: Check if `src/core/db.ts` matches the migrations in `src/core/migrations.ts`. Ensure no data loss.
3. **Tool Loop**: Ensure `src/agent/executor.ts` correctly handles recursive calls for `deep_search`, `start_mission`, and `pc_command`.
4. **Recursive Reasoning**: Optimize `intelligence/pro_search.ts` to prevent infinite loops and ensure citations are ALWAYS generated.
5. **Safety Enforcer**: Double-check `src/agent/policies/enforcer.ts`. It must be the final gatekeeper before ANY command hits the local PC.

---

## 🎨 DESIGN MANIFESTO (APPLE + PERPLEXITY STYLE)
*   **Minimalism**: Remove all clutter. Use thin lines, large paddings, and Lucide icons.
*   **Aesthetics**: Glassmorphism (blur: 20px), Deep Black (#050507), Accent Purple (#6c63ff).
*   **Animations**: Framer Motion for React elements. Page transitions must be smooth.
*   **Components**: 
    *   **Sidebar**: Collapsible, icon-based.
    *   **Sidepanel**: Floating-effect, contains AI chat.
    *   **Cards**: Rounded (16px), subtle borders.

---

## 🚀 FEATURE COMPLETION TASKS
1. **Browser Data Sync**: Implement real-time history sync between the Browser and the Telegram Mini-app.
2. **Mission UI**: Build a "Mission Dashboard" in the Portal to show progress bars for background AI tasks.
3. **Voice Command Bridge**: Link the `VoiceGateway` in Python to the `transcribeVoice` tool in the Cloud.
4. **Auto-Updater**: Design a logic for the Browser to check for GitHub releases.

---

## 🛠 HOW TO START
1. Read ALL files in `src/`, `intelligence/`, `evolution/`, `portal/`, `apps/browser/`, and `pc_agent/`.
2. Identify missing imports or logic gaps.
3. Apply the "Elite Design" to all HTML/React files.
4. Return the fully functional, beautiful code.
